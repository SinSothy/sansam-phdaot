/**
 * Global API Configuration
 */
export const API_CONFIG = {
  VERSION: "1.0.0",
  PLATFORM: "web",
  // Default values or env fallbacks
  DEFAULT_TIMEZONE: "Asia/Phnom_Penh",
  // In a real app, this might be injected via build env
  AUTH_STORAGE_KEY: "phdaot_auth_user",
};
